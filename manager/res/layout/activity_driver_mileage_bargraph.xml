<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#FFF"
    android:orientation="vertical">

    <android.support.v7.widget.Toolbar
        android:layout_height="50dp"
        android:layout_width="match_parent"
        android:id="@+id/tool_bar"
        android:background="@color/toolbar_color"
        android:elevation="2dp"
        xmlns:app="http://schemas.android.com/apk/res-auto"
        xmlns:android="http://schemas.android.com/apk/res/android"
        app:popupTheme="@style/ThemeOverlay.YourApp"
        app:titleTextAppearance="@style/ToolbarTitle"
        android:theme="@style/toolBarStyle">

        <ImageView
            android:id="@+id/driverDistancePieChartPdfReport1"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="right"
            android:src="@drawable/white_pdf"
            android:layout_marginTop="18dp"
            android:layout_marginRight="15dp" />
    </android.support.v7.widget.Toolbar>

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="1dp"
        android:background="@color/toolbar_color"></LinearLayout>

    <LinearLayout
        android:id="@+id/driverMileageBarGraphContent"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_marginTop="10sp"
        android:layout_weight="1"
        android:orientation="vertical" />

    <ImageView
        android:id="@+id/driverMileageBarGraphRefresh"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="right"
        android:layout_marginBottom="10dp"
        android:layout_marginRight="10dp"
        android:src="@drawable/refresh" />

</LinearLayout>